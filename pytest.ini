[pytest]
testpaths = tests
addopts = -q
filterwarnings =
    ; Emise par le TestClient de Starlette, hors de notre controle.
    ignore:The anyio.abc.BlockingPortal alias is deprecated:DeprecationWarning
